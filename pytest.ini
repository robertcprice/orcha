[pytest]
# Test discovery
testpaths = tests

# Skip directories that contain interactive/manual scripts named like tests.
norecursedirs =
    archive
    orchestrator
    obsidian-vault
    web-ui
    node_modules
    venv
    .venv
    .claude
    github-observability-reference
    examples
    docs
